thorntail:
  datasources:
    data-sources:
      MessageDS:
        driver-name: postgresql
        connection-url: jdbc:postgresql://message-postgresql:5432/message
        user-name: message
        password: message